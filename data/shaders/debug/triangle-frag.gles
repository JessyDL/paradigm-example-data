#version 300 es
precision mediump float;
precision highp int;

uniform highp sampler2D bindGSampler;

layout(location = 0) out highp vec4 outColor;
in highp vec2 intex;
in highp vec4 incolor;

void main()
{
    outColor = vec4(texture(bindGSampler, intex).xyz, 1.0);
}


#version 300 es
precision mediump float;
precision highp int;

uniform highp sampler2D GSampler;

layout(location = 0) out highp vec4 outColor;
in highp vec2 vstex;
in highp vec4 vscolor;

void main()
{
    outColor = vec4(texture(GSampler, vstex).xyz, 1.0);
}

